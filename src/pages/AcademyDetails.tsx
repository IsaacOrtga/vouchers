import {useLocation, useParams } from 'react-router-dom';
import "../styles/home.css";

const AcademyDetails = () => {
    // const location = useLocation();
    // const { id } = useParams();

    

  return (
    <div>
        <h1 style={{color: 'black'}}>aquí la información</h1>
    </div>
  )
}

export default AcademyDetails
